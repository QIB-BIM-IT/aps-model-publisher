{
  "name": "aps-model-publisher",
  "version": "1.0.0",
  "private": true,
  "description": "Application de publication de modèles APS/ACC",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "install:backend": "cd backend && npm cache clean --force && npm install --prefer-online",
    "install:frontend": "cd frontend && npm install --production=false",
    "build:frontend": "cd frontend && npm run build",
    "build": "cd frontend && npm run build",
    "start": "node backend/src/server.js",
    "postinstall": "npm run install:backend && npm run install:frontend && npm run build"
  }
}